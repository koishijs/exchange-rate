name: Publish

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  publish:
    permissions:
      id-token: write
      contents: read
    uses: cordiverse/workflows/.github/workflows/publish.yml@main